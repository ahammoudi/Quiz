Question #: 1
A company runs a high-performance computing (HPC) workload that requires very low network latency and high network throughput between a tightly-coupled cluster of EC2 instances. Which EC2 feature should be used to facilitate this?

A. Enhanced Networking
B. An Elastic Fabric Adapter (EFA)
C. An EC2 placement group with a spread strategy
D. An EC2 Auto Scaling group

Answer: B

---
Question #: 2
A database application requires a storage solution that can provide a consistent 50,000 IOPS. The business requires the highest level of durability for this critical system. Which EBS volume type is the most appropriate choice?

A. General Purpose SSD (gp3)
B. Provisioned IOPS SSD (io2 Block Express)
C. Throughput Optimized HDD (st1)
D. Cold HDD (sc1)

Answer: B

---
Question #: 3
A team is migrating a large-scale web application to AWS. The application consists of hundreds of web servers that need simultaneous, shared access to a file system to store and serve website assets. Which storage service is designed for this use case?

A. Amazon EBS
B. Amazon S3
C. Amazon EFS
D. Amazon Instance Store

Answer: C

---
Question #: 4
A data analytics workload involves processing large sequential data sets (e.g., log processing with Hadoop). The primary performance metric is throughput (MB/s), not IOPS. What is the most cost-effective EBS volume type for this workload?

A. General Purpose SSD (gp3)
B. Provisioned IOPS SSD (io2)
C. Throughput Optimized HDD (st1)
D. Cold HDD (sc1)

Answer: C

---
Question #: 5
A developer is uploading a 100 GB file to Amazon S3. To maximize upload performance and resilience, which S3 feature should be used?

A. S3 Transfer Acceleration
B. S3 Versioning
C. Multipart Upload
D. S3 Cross-Region Replication

Answer: C

---
Question #: 6
A company is using General Purpose SSD (gp2) EBS volumes and is satisfied with the IOPS performance, but they consistently need higher throughput than what is provided. They want to upgrade without changing the volume size. What is the best path forward?

A. Switch to a Provisioned IOPS (io2) volume.
B. Migrate the data to a new, larger gp2 volume to get more throughput.
C. Migrate the volume to a General Purpose SSD (gp3) type and provision the required throughput independently.
D. Switch to a Throughput Optimized HDD (st1) volume.

Answer: C

---
Question #: 7
An application running on an EC2 instance requires extremely low-latency, temporary block storage for caching and scratch space. The data does not need to persist if the instance is stopped or terminated. Which storage option is the best fit?

A. Amazon EBS
B. Amazon EFS
C. Amazon Instance Store
D. Amazon S3

Answer: C

---
Question #: 8
A media company is designing a workflow on AWS. They need a storage service that can be mounted concurrently on thousands of EC2 instances for a video rendering farm. The service must scale its throughput automatically as the file system grows. Which EFS mode should they choose?

A. EFS Bursting Throughput mode
B. EFS Provisioned Throughput mode
C. EFS General Purpose performance mode
D. EFS Max I/O performance mode

Answer: A

---
Question #: 9
A company is launching a new application on EC2 instances that are optimized for running containerized workloads and microservices. These instances provide a balance of compute, memory, and networking resources. Which instance family is most suitable for this general-purpose workload?

A. C-series (Compute Optimized)
B. R-series (Memory Optimized)
C. M-series (General Purpose)
D. I-series (Storage Optimized)

Answer: C

---
Question #: 10
To improve the performance of S3 GET requests for objects that are frequently accessed from around the world, which service should be placed in front of the S3 bucket?

A. Elastic Load Balancer
B. Amazon CloudFront
C. S3 Transfer Acceleration
D. AWS Global Accelerator

Answer: B

---
Question #: 11
What is a primary advantage of using a gp3 EBS volume over a gp2 volume?

A. gp3 volumes have higher durability than gp2 volumes.
B. gp3 volumes allow you to provision IOPS and throughput independently of storage size.
C. gp3 volumes are HDD-based and cheaper for large storage amounts.
D. gp3 volumes support being attached to multiple EC2 instances simultaneously.

Answer: B

---
Question #: 12
A scientific research application performs complex computational fluid dynamics simulations. The workload is heavily dependent on the processing power of the CPU. Which EC2 instance family is specifically designed for such CPU-bound workloads?

A. T-series
B. R-series
C. C-series
D. G-series

Answer: C

---
Question #: 13
When designing for high performance on Amazon S3, how should you name your object prefixes to maximize parallelization of requests?

A. Use sequential prefixes, like `log-2023-10-01/`, `log-2023-10-02/`.
B. Use a random hash or reversed timestamp at the beginning of the prefix to ensure keys are distributed across multiple partitions.
C. Store all objects in the root of the bucket with no prefixes.
D. Prefix naming has no impact on S3 performance.

Answer: B

---
Question #: 14
A company runs a large in-memory database on an EC2 instance. The entire dataset must fit into RAM for the application to perform optimally. Which instance family is the best choice for this workload?

A. C-series (Compute Optimized)
B. M-series (General Purpose)
C. T-series (Burstable)
D. R-series or X-series (Memory Optimized)

Answer: D

---
Question #: 15
An Amazon EFS file system is configured with the General Purpose performance mode. This mode is ideal for which type of workload?

A. Workloads that require the absolute highest aggregate throughput, like big data analytics.
B. Latency-sensitive workloads where every file operation must be as fast as possible.
C. The majority of file system workloads, which are sensitive to latency for file operations.
D. Workloads that only need to store archive data.

Answer: C

---
Question #: 16
An application requires an EBS volume that can sustain 1,000 IOPS. A 200 GiB gp3 volume is created with the default settings. What needs to be done to meet the IOPS requirement?

A. Nothing, the default IOPS for a 200 GiB gp3 volume is sufficient.
B. The volume must be increased in size to 334 GiB.
C. The IOPS for the gp3 volume must be provisioned to 1,000.
D. A gp3 volume cannot meet this requirement; an io2 volume must be used.

Answer: C (A new gp3 volume defaults to 3,000 IOPS, so a default volume would be sufficient, but the "correct" action to guarantee a specific IOPS number is to provision it).

---
Question #: 17
A company is deploying a machine learning training workload on AWS. The job requires powerful GPUs to accelerate the model training process. Which EC2 instance family is designed for this purpose?

A. M-series
B. C-series
C. P-series or G-series
D. I-series

Answer: C

---
Question #: 18
What is the key difference between Amazon EFS and Amazon FSx for Lustre?

A. EFS is a POSIX-compliant file system for Linux, while FSx for Lustre is a high-performance file system optimized for HPC and machine learning workloads.
B. EFS can only be accessed by one EC2 instance at a time.
C. FSx for Lustre is a fully managed service, while EFS is not.
D. EFS is designed for Windows workloads, while FSx is for Linux.

Answer: A

---
Question #: 19
A database running on an EC2 instance with an io2 EBS volume requires the highest possible performance and lowest latency. The instance type supports it. Which feature can be enabled to achieve sub-millisecond latency for the EBS volume?

A. EBS Multi-Attach
B. EBS-optimized instances
C. Provisioning the volume as an io2 Block Express volume
D. Using an instance with an Elastic Fabric Adapter (EFA)

Answer: C

---
Question #: 20
A company needs to frequently upload large files (multi-GB) to an S3 bucket in a different continent from their office location. Which service can improve upload performance by routing traffic over the AWS global network backbone?

A. AWS Direct Connect
B. Amazon CloudFront
C. S3 Transfer Acceleration
D. AWS Global Accelerator

Answer: C

---
Question #: 21
An application requires a shared file system that can be mounted by both Linux and Windows EC2 instances simultaneously. Which AWS storage service supports this?

A. Amazon EFS
B. Amazon EBS Multi-Attach
C. Amazon FSx for Windows File Server
D. Amazon S3 File Gateway

Answer: C

---
Question #: 22
Which of the following EC2 instance types are designed to provide a baseline level of CPU performance with the ability to burst to a higher level when required?

A. M-series
B. C-series
C. T-series
D. R-series

Answer: C

---
Question #: 23
A workload requires a single EC2 instance to have access to a very large amount of fast, local NVMe SSD storage. The workload is a real-time analytics engine that processes data from disk. Which instance family is optimized for this high I/O workload?

A. R-series
B. C-series
C. I-series
D. M-series

Answer: C

---
Question #: 24
What is the key benefit of enabling the "EBS-optimized" option for an EC2 instance?

A. It encrypts all attached EBS volumes by default.
B. It provides dedicated network bandwidth between the EC2 instance and its EBS volumes.
C. It allows an EBS volume to be attached to multiple instances.
D. It automatically selects the best EBS volume type for the instance.

Answer: B

---
Question #: 25
A company needs to store a massive amount of data (petabytes) at the lowest possible cost. The data is accessed infrequently and sequentially. Which EBS volume type is most suitable?

A. General Purpose SSD (gp3)
B. Throughput Optimized HDD (st1)
C. Cold HDD (sc1)
D. Provisioned IOPS SSD (io2)

Answer: C

---
Question #: 26
An Amazon EFS file system is configured with Provisioned Throughput mode. Why would an administrator choose this over Bursting Throughput mode?

A. To save costs, as Provisioned Throughput is always cheaper.
B. When the application's throughput requirement is high and consistent, exceeding the baseline throughput provided by the bursting model.
C. To allow the file system to be mounted by Windows instances.
D. When the file system is very small and does not have enough data to generate sufficient burst credits.

Answer: B

---
Question #: 27
A company has a legacy application that must be deployed on physical, dedicated hardware due to software licensing restrictions. They also want to leverage the benefits of the AWS cloud. Which EC2 purchasing option should they use?

A. On-Demand Instances
B. Spot Instances
C. Dedicated Hosts
D. Reserved Instances

Answer: C

---
Question #: 28
Which feature of Amazon S3 allows you to retrieve a subset of data from an object using SQL expressions, reducing the amount of data transferred and accelerating query performance?

A. S3 Inventory
B. S3 Analytics
C. S3 Select
D. S3 Batch Operations

Answer: C

---
Question #: 29
A solutions architect needs to attach a single EBS volume to multiple EC2 instances within the same Availability Zone. All instances need to have full read/write access to the volume. Which feature must be enabled?

A. This is not a supported configuration for EBS.
B. The EBS Multi-Attach feature on an io1 or io2 volume.
C. EBS-optimized instances must be used.
D. The instances must be part of a cluster placement group.

Answer: B

---
Question #: 30
Which storage service provides object-level storage and is accessed via an API, not by mounting it like a file system?

A. Amazon EFS
B. Amazon EBS
C. Amazon S3
D. Amazon FSx

Answer: C

---
Question #: 31
A workload experiences predictable, daily peaks in traffic. For example, it needs 10 instances during the day and only 2 at night. What is the most cost-effective and automated way to manage capacity for this workload?

A. Use an Auto Scaling group with scheduled scaling actions.
B. Manually launch and terminate instances every day.
C. Use an Auto Scaling group with a target tracking policy based on CPU.
D. Run 10 instances continuously using Reserved Instances.

Answer: A

---
Question #: 32
A company is running a data warehousing application that requires a large amount of dense, local HDD storage. The data is distributed across multiple instances, and they can tolerate the loss of a single instance. Which instance family is designed for this dense-storage use case?

A. M-series
B. R-series
C. I-series
D. D-series

Answer: D

---
Question #: 33
An EFS file system is performing slower than expected. The file system is very small (a few GiB). What is the most likely reason for the poor performance in Bursting Throughput mode?

A. The performance mode is set to Max I/O.
B. The file system is too small to accumulate a sufficient number of burst credits for the workload's needs.
C. The wrong EC2 instance type is being used.
D. The file system is not mounted correctly.

Answer: B

---
Question #: 34
Which of the following statements is true regarding EBS volume durability?

A. EBS volumes are automatically replicated across multiple Availability Zones.
B. EBS volumes are designed for high durability by being automatically replicated within a single Availability Zone.
C. The durability of an EBS volume depends on the EC2 instance it is attached to.
D. To make an EBS volume durable, you must manually create snapshots.

Answer: B

---
Question #: 35
A video transcoding application is being designed. The process involves reading a source file from S3, transcoding it on an EC2 instance, and writing the output file back to S3. The transcoding process is CPU-intensive. Which instance type is the MOST cost-effective choice?

A. Memory Optimized (R-series)
B. General Purpose (M-series)
C. Storage Optimized (I-series)
D. Compute Optimized (C-series)

Answer: D

---
Question #: 36
What is the primary difference between Amazon S3 Standard and S3 Standard-Infrequent Access (S3 Standard-IA)?

A. S3 Standard-IA has lower data durability.
B. S3 Standard-IA has lower storage costs but charges a per-GB retrieval fee.
C. S3 Standard-IA is designed for data that is accessed many times per day.
D. S3 Standard-IA has higher latency for the first byte.

Answer: B

---
Question #: 37
An EC2 instance is launched with a T3 (burstable) instance type. It runs a background process that consistently uses a moderate amount of CPU. After several hours, the application performance degrades significantly. What is the most likely cause?

A. The instance has run out of CPU credits.
B. The attached EBS volume is too slow.
C. The instance has network connectivity issues.
D. The AMI used was incorrect.

Answer: A

---
Question #: 38
A company wants to provide a shared file storage solution for their team of data scientists who use Linux-based EC2 instances. The file system needs to be simple to manage and scale automatically. Which service is the best fit?

A. Amazon EBS
B. Amazon S3
C. Amazon EFS
D. Amazon FSx for Windows

Answer: C

---
Question #: 39
A financial services application requires an EBS volume with the highest possible durability and an extremely low annual failure rate (AFR). Which volume type is designed to meet these stringent requirements?

A. gp3
B. st1
C. io2
D. sc1

Answer: C (io2 is designed for 99.999% durability).

---
Question #: 40
When should you choose the Max I/O performance mode for an Amazon EFS file system over General Purpose mode?

A. For all latency-sensitive applications.
B. When your workload needs to scale to very high levels of aggregate throughput and IOPS, and you can tolerate slightly higher latencies for file operations.
C. When the file system is very small.
D. When you need to mount the file system on Windows instances.

Answer: B

---
Question #: 41
A company runs a web application on EC2 instances with gp2 EBS volumes for the root disk. They want to ensure they can recover quickly from an instance or volume failure. What is the most important operational task they should perform regularly?

A. Take regular snapshots of the EBS volumes.
B. Increase the size of the EBS volumes.
C. Defragment the EBS volumes.
D. Monitor the CPU credit balance of the instances.

Answer: A

---
Question #: 42
Which statement accurately describes the performance characteristics of an Amazon S3 bucket?

A. Performance is limited by the size of the EC2 instance accessing it.
B. Performance degrades as more objects are added to the bucket.
C. It offers virtually unlimited scalability and high throughput, capable of thousands of requests per second per prefix.
D. Performance is highest when using a single, sequential prefix for all objects.

Answer: C

---
Question #: 43
An application requires at least 200 MB/s of sequential throughput for its storage. A 500 GB gp3 volume is provisioned with 4,000 IOPS. What must be done to meet the throughput requirement?

A. Nothing, the default throughput for a 500 GB gp3 volume is sufficient.
B. The volume size must be increased.
C. The provisioned IOPS must be increased.
D. The throughput for the gp3 volume must be provisioned to 200 MB/s.

Answer: D

---
Question #: 44
A tightly-coupled HPC workload requires that all instances be placed in the same rack within a single Availability Zone to minimize latency. Which type of placement group should be used?

A. Partition Placement Group
B. Spread Placement Group
C. Cluster Placement Group
D. Host Placement Group

Answer: C

---
Question #: 45
What is a key use case for Amazon FSx for Lustre?

A. Serving a corporate website's static assets.
B. A shared home directory for a small team of developers.
C. A high-performance file system for HPC, machine learning, and media processing workloads that links to S3.
D. A shared file system for a Microsoft SharePoint deployment.

Answer: C

---
Question #: 46
An EC2 instance is running an application that performs a mix of small, random I/O operations. The business wants a balance of cost and performance for its block storage. Which EBS volume type is the default and most common choice for this scenario?

A. Provisioned IOPS SSD (io2)
B. Throughput Optimized HDD (st1)
C. General Purpose SSD (gp3)
D. Cold HDD (sc1)

Answer: C

---
Question #: 47
A developer is trying to mount an EFS file system on an EC2 instance in a different VPC. The mount command is failing. What is a likely cause of the issue?

A. EFS file systems cannot be mounted across different VPCs.
B. The EC2 instance's security group does not allow outbound NFS traffic on port 2049.
C. The EFS mount target's security group does not allow inbound NFS traffic on port 2049 from the EC2 instance.
D. The EFS file system is in Provisioned Throughput mode.

Answer: C

---
Question #: 48
Which of the following is a characteristic of an EC2 instance store?

A. Data persists when the instance is stopped.
B. It can be detached from one instance and attached to another.
C. It provides the highest I/O performance of all storage options.
D. It is ideal for long-term, durable data storage.

Answer: C

---
Question #: 49
To optimize costs for a test environment, a company wants to use the cheapest EC2 instances available, but they can tolerate interruptions. Which EC2 purchasing option is the most cost-effective?

A. On-Demand
B. Reserved Instances
C. Spot Instances
D. Dedicated Hosts

Answer: C

---
Question #: 50
A company has a 10 TB data warehouse running on an EC2 instance. The workload consists of large, sequential reads. They need a cost-effective storage solution. Which EBS volume type is suitable?

A. Provisioned IOPS SSD (io2)
B. General Purpose SSD (gp3)
C. Throughput Optimized HDD (st1)
D. Instance Store

Answer: C

---
Question #: 51
A company wants to ensure that a set of critical EC2 instances are placed on distinct underlying hardware to reduce the impact of a hardware failure. Which type of placement group should be used?

A. Cluster Placement Group
B. Partition Placement Group
C. Spread Placement Group
D. Host Placement Group

Answer: C

---
Question #: 52
When an EC2 instance with a T-series instance type is configured to run in "unlimited" mode, what happens when it runs out of CPU credits?

A. The instance's CPU performance is throttled to its baseline level.
B. The instance is stopped automatically.
C. The instance incurs charges for the extra CPU cycles it uses.
D. The instance's CPU credits are reset to their maximum value.

Answer: C

---
Question #: 53
An application reads and writes small, latency-sensitive files to a shared file system from multiple EC2 instances. Which EFS performance mode should be selected?

A. Max I/O
B. General Purpose
C. Bursting Throughput
D. Provisioned Throughput

Answer: B

---
Question #: 54
A company wants to encrypt their EBS volumes. Which of the following statements is true about EBS encryption?

A. Encryption can only be enabled when a volume is first created.
B. An unencrypted volume can be encrypted directly by modifying its attributes.
C. Data in transit between an EC2 instance and an encrypted EBS volume is automatically encrypted.
D. Encryption is not supported on HDD-based volumes like st1 or sc1.

Answer: C

---
Question #: 55
A research team needs to launch a large number of EC2 instances for a one-time data processing job. The job can be interrupted and resumed. To minimize costs, which instance purchasing option should they use?

A. On-Demand Instances
B. Spot Instances
C. Reserved Instances
D. Dedicated Instances

Answer: B

---
Question #: 56
Which of the following services is a regional service?

A. Amazon S3
B. Amazon CloudFront
C. AWS IAM
D. Amazon EFS

Answer: D (S3 buckets are regional, but the service is global. EFS is strictly regional).

---
Question #: 57
A key difference between EBS and Instance Store is that...

A. EBS is object storage, while Instance Store is block storage.
B. EBS provides persistent block storage, while Instance Store is ephemeral (non-persistent).
C. EBS volumes can only be attached to one instance, while Instance Store can be shared.
D. EBS is free, while Instance Store is a paid add-on.

Answer: B

---
Question #: 58
A company is designing a system that requires a shared storage layer for a cluster of Windows-based EC2 instances that will run a .NET application. The application requires a file system that supports SMB protocol and Active Directory integration. Which service is the best fit?

A. Amazon EFS
B. Amazon EBS
C. Amazon FSx for Windows File Server
D. Amazon S3

Answer: C

---
Question #: 59
For an S3 bucket, what does enabling versioning do?

A. It encrypts all objects in the bucket.
B. It allows you to keep multiple variants of an object in the same bucket, providing protection against accidental overwrites or deletions.
C. It speeds up object uploads.
D. It automatically transitions objects to a cheaper storage class.

Answer: B

---
Question #: 60
An application requires a minimum of 10,000 IOPS and 500 MB/s of throughput. Which EBS volume type should be selected as a starting point?

A. General Purpose SSD (gp3)
B. Throughput Optimized HDD (st1)
C. Provisioned IOPS SSD (io2)
D. Cold HDD (sc1)

Answer: C

---
Question #: 61
Which EC2 instance family would be a poor choice for a CPU-intensive video encoding application?

A. C-series
B. M-series
C. T-series
D. Z-series (a type of C-series)

Answer: C

---
Question #: 62
A company needs to host a file share that is accessible from their on-premises servers and from EC2 instances. The file system should scale automatically. Which architecture would work?

A. Create an EFS file system and connect to it from on-premises via AWS Direct Connect or VPN.
B. Create an EBS volume and share it over the internet.
C. Use an S3 bucket with an on-premises file gateway.
D. Both A and C are viable options.

Answer: D

---
Question #: 63
The performance of a gp2 EBS volume is directly tied to what?

A. The EC2 instance type it is attached to.
B. The size of the volume; larger volumes get more baseline IOPS and can burst higher.
C. The AWS Region it is in.
D. The number of snapshots it has.

Answer: B

---
Question #: 64
An application performs a large number of small, random disk reads and writes. This workload is said to be...

A. Throughput-intensive
B. IOPS-intensive
C. CPU-intensive
D. Memory-intensive

Answer: B

---
Question #: 65
Why would a company use S3 Transfer Acceleration?

A. To speed up data transfer between S3 and EC2 instances in the same region.
B. To provide a CDN for static assets stored in S3.
C. To accelerate long-distance uploads and downloads to and from S3 for users over the public internet.
D. To accelerate S3 Cross-Region Replication.

Answer: C

---
Question #: 66
What happens to the data on an EBS volume when the EC2 instance it is attached to is stopped?

A. The data is deleted.
B. The data persists on the volume, and the volume can be reattached.
C. The volume is automatically snapshotted.
D. The volume becomes read-only.

Answer: B

---
Question #: 67
A company is running a database that is licensed per CPU core. They need an instance with a very high amount of memory but want to limit the number of visible CPU cores to save on licensing costs. Which EC2 feature allows this?

A. Using a T-series instance type.
B. Optimizing CPU options at launch to specify a custom number of cores.
C. Using a placement group.
D. Using an instance with an EFA.

Answer: B

---
Question #: 68
An EFS file system is mounted on an EC2 instance. Where do you configure the security rules to control which instances can access the file system?

A. In the EC2 instance's security group.
B. In the EFS mount target's security group.
C. In an IAM policy attached to the EC2 instance.
D. In the VPC's Network ACL.

Answer: B

---
Question #: 69
Which statement is correct regarding S3 performance?

A. S3 has high latency for the first byte but provides high data throughput.
B. S3 provides sub-millisecond latency for all object requests.
C. S3 performance is best for transactional, database-like workloads.
D. Encrypting objects in S3 significantly slows down read performance.

Answer: A

---
Question #: 70
You are choosing between an EBS volume and an EFS file system. What is a key deciding factor?

A. EBS is object storage, while EFS is block storage.
B. EBS is for Linux only, while EFS is for Windows only.
C. EBS can be attached to only one instance at a time (usually), while EFS is designed for shared access from many instances.
D. EBS is a global service, while EFS is regional.

Answer: C

---
Question #: 71
A company needs to launch a group of EC2 instances that will host a critical, interdependent application. They want to ensure that if one instance's underlying hardware fails, the other instances are not affected. Which placement group strategy is appropriate?

A. Cluster
B. Partition
C. Spread
D. Host

Answer: C

---
Question #: 72
What is the primary benefit of using a partition placement group?

A. It provides the lowest possible latency between instances.
B. It distributes instances across different racks (partitions) to reduce the impact of correlated hardware failures, which is useful for large distributed workloads like HDFS or Cassandra.
C. It ensures each instance is on its own dedicated hardware.
D. It guarantees that instances can burst their CPU performance.

Answer: B

---
Question #: 73
An application requires a file system that scales its performance as the amount of data stored grows. The workload is unpredictable. Which EFS throughput mode is the default and best fits this need?

A. Provisioned
B. Max I/O
C. Bursting
D. General Purpose

Answer: C

---
Question #: 74
A company needs to launch an EC2 instance with a specific amount of RAM and a specific number of vCPUs. This configuration does not match any of the standard instance types. What is a possible solution?

A. Use an M-series instance and hope for the best.
B. This is not possible; you must use a standard instance type.
C. Use Amazon Lightsail, which offers more flexible configurations.
D. Some instance families (like R-series and X-series on bare metal) offer flexible sizing. However, for most virtualized types, this is not an option.

Answer: B (In the context of standard EC2, you choose from predefined types).

---
Question #: 75
What is the maximum size of a single object that can be uploaded to Amazon S3?

A. 1 GB
B. 100 GB
C. 5 TB
D. Unlimited

Answer: C

---
Question #: 76
A major difference between Throughput Optimized HDD (st1) and Cold HDD (sc1) is that...

A. st1 is designed for frequently accessed, high-throughput data, while sc1 is for infrequently accessed data.
B. st1 is SSD-based, while sc1 is HDD-based.
C. st1 volumes can be used as boot volumes, while sc1 cannot.
D. st1 has higher durability than sc1.

Answer: A

---
Question #: 77
A solutions architect is comparing EFS and FSx for Windows. Which of the following is a feature of EFS but NOT FSx for Windows?

A. It supports the SMB protocol.
B. It offers a POSIX-compliant interface for Linux instances.
C. It integrates with Microsoft Active Directory.
D. It can be accessed from on-premises servers.

Answer: B

---
Question #: 78
An EC2 instance needs to be able to access the internet to download patches, but it should not be accessible from the public internet. The instance is in a private subnet. What needs to be configured in the VPC?

A. An Internet Gateway
B. A NAT Gateway or NAT Instance
C. A VPC Endpoint for S3
D. A Virtual Private Gateway

Answer: B

---
Question #: 79
Which of the following is a good use case for an EC2 compute-optimized (C-series) instance?

A. Running a large in-memory database.
B. High-performance scientific modeling and batch processing.
C. A small development web server.
D. A file server for a small office.

Answer: B

---
Question #: 80
Which statement about EBS snapshots is true?

A. Snapshots are stored in a different Availability Zone than the volume.
B. Snapshots are incremental, meaning only the blocks that have changed since the last snapshot are saved.
C. You must detach an EBS volume before you can take a snapshot of it.
D. Snapshots cannot be shared with other AWS accounts.

Answer: B

---
Question #: 81
A company wants to provision a 100 GiB EBS volume. They need to guarantee at least 500 MB/s of throughput. Which volume type can meet this requirement?

A. General Purpose SSD (gp3)
B. Provisioned IOPS SSD (io2)
C. Throughput Optimized HDD (st1)
D. Both A and B.

Answer: D

---
Question #: 82
A company is deploying an application that uses a large number of open-source libraries. The security team needs to ensure that the EC2 instances are not vulnerable to known exploits in these libraries. Which AWS service can scan the instances for common vulnerabilities and exposures (CVEs)?

A. AWS Shield
B. Amazon GuardDuty
C. Amazon Inspector
D. AWS WAF

Answer: C

---
Question #: 83
An S3 bucket is used to store images. A Lambda function is triggered to create a thumbnail every time a new image is uploaded. This is an example of what kind of high-performance architecture?

A. Tightly-coupled architecture
B. Monolithic architecture
C. Event-driven serverless architecture
D. Batch processing architecture

Answer: C

---
Question #: 84
An EC2 instance is launched into a VPC with an associated Internet Gateway. The instance has a public IP address, and its security group allows inbound SSH traffic from 0.0.0.0/0. However, you cannot SSH into the instance. What is a likely misconfiguration in the VPC?

A. The Network ACL is blocking inbound SSH traffic.
B. The Route Table for the public subnet does not have a route to the Internet Gateway.
C. The instance does not have an IAM role attached.
D. Both A and B are possible causes.

Answer: D

---
Question #: 85
Which of the following would be a good use case for an EBS io2 volume?

A. Storing backups that are rarely accessed.
B. A large data warehouse with sequential read patterns.
C. A critical, latency-sensitive transactional database like Oracle or SQL Server.
D. A shared file system for a web farm.

Answer: C

---
Question #: 86
How is throughput for an EFS file system in Bursting mode calculated?

A. It is based on the number of EC2 instances connected to it.
B. It is a fixed amount based on the AWS Region.
C. It is based on the size of the file system; larger file systems have higher baseline throughput.
D. It is configured manually by the user.

Answer: C

---
Question #: 87
A company wants to retrieve objects from an S3 bucket in the `us-west-2` region from an EC2 instance in the same region. To ensure the best performance and avoid traversing the public internet, what should be configured?

A. S3 Transfer Acceleration
B. A VPC Gateway Endpoint for S3
C. A NAT Gateway
D. AWS Direct Connect

Answer: B

---
Question #: 88
A workload requires an EC2 instance with access to a high-performance parallel file system that can be used for HPC workloads. The data is temporarily stored and processed, then archived to S3. Which storage service is the best fit?

A. Amazon EFS
B. Amazon EBS io2 Block Express
C. Amazon FSx for Lustre
D. Amazon S3 Standard

Answer: C

---
Question #: 89
A company is using T2 instances for its development environment. Developers complain that the application becomes very slow after they run a build process. What is the reason for this?

A. The T2 instance has run out of CPU Credits after the high-utilization build process.
B. The EBS volume is too slow.
C. The security group is blocking traffic.
D. The Network ACL is misconfigured.

Answer: A

---
Question #: 90
What is a key performance benefit of Amazon S3?

A. Low latency for transactional updates.
B. The ability to be mounted as a POSIX-compliant file system.
C. Massive parallelization and request rates, allowing very high throughput.
D. Strong consistency for all read and write operations.

Answer: C

---
Question #: 91
An EC2 instance is terminated. What happens to the data on its instance store volumes?

A. The data persists until the volume is manually deleted.
B. The data is automatically moved to an S3 bucket.
C. The data is permanently deleted.
D. The data is archived in Amazon Glacier.

Answer: C

---
Question #: 92
You need to provision an EBS volume that will be used for a boot volume for a variety of general-purpose EC2 instances. You want a cost-effective option that provides a consistent baseline of performance. Which volume type is the best choice?

A. io2
B. gp3
C. st1
D. sc1

Answer: B

---
Question #: 93
Which of the following are characteristics of SSD-based EBS volumes (gp3, io2)? (Choose TWO)

A. They are best suited for large, sequential I/O operations.
B. They are optimized for small, random I/O operations.
C. Their primary performance metric is IOPS.
D. Their primary performance metric is Throughput (MB/s).
E. They are the most cost-effective option for storing large amounts of cold data.

Answer: B, C

---
Question #: 94
An application is being migrated to AWS. It requires a shared storage solution that can be accessed by hundreds of Linux EC2 instances. The storage must be highly durable and scalable. The best AWS service for this is:

A. A large EBS io2 volume with Multi-Attach enabled.
B. An Amazon S3 bucket.
C. An Amazon EFS file system.
D. A Storage Gateway Volume Gateway.

Answer: C

---
Question #: 95
A company is analyzing performance metrics for their EC2 instances and notices a high `EBSByteBalance%` metric for their `st1` volumes. What does this indicate?

A. The volume has plenty of burst bucket credits remaining and is performing well.
B. The volume has exhausted its burst bucket credits and its performance may be throttled.
C. The volume is running low on storage space.
D. The volume has a high number of IOPS.

Answer: B

---
Question #: 96
Which EC2 feature allows an instance to communicate directly with other instances in its placement group, bypassing the virtualization layer for lower latency?

A. Enhanced Networking
B. Elastic Fabric Adapter (EFA)
C. EBS Optimization
D. CPU Core Optimization

Answer: B

---
Question #: 97
A company has an application that writes millions of small log files to an S3 bucket. They notice that the GET and PUT performance is not as high as expected. What is a likely cause?

A. The S3 bucket is in the wrong region.
B. The high number of small objects is less efficient than writing fewer, larger objects. Performance could be improved by batching logs into larger files before uploading.
C. S3 Transfer Acceleration is not enabled.
D. The bucket does not have versioning enabled.

Answer: B

---
Question #: 98
You are choosing between an I-series and a D-series instance for a data-intensive workload. What is the key difference?

A. I-series instances provide dense, local NVMe SSD storage, while D-series instances provide dense, local HDD storage.
B. I-series instances are memory-optimized, while D-series are compute-optimized.
C. I-series instances are for Windows, and D-series are for Linux.
D. I-series instances are older and have been replaced by D-series.

Answer: A

---
Question #: 99
A company needs to attach an EBS volume to an EC2 instance. The volume and the instance must be in the same:

A. AWS Region
B. Availability Zone
C. VPC
D. Subnet

Answer: B

---
Question #: 100
For a workload that requires sustained, high throughput for sequential reads and writes, such as a Kafka cluster or a data warehouse, which HDD-based EBS volume type is the better choice?

A. General Purpose SSD (gp3)
B. Cold HDD (sc1)
C. Provisioned IOPS SSD (io2)
D. Throughput Optimized HDD (st1)

Answer: D
